import React from "react";
import classes from "./Header.module.scss";

const Header = () => {
  return (
    <header>
      <h1 className={classes.title}>Español Flashcards</h1>
    </header>
  );
};

export default Header;
